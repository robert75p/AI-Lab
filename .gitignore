# Dependencies
node_modules/

# Secrets — never commit real credentials
.env

# Recon output: dated run folders, manifest, and raw mock data.
# Keep the directory itself tracked via .gitkeep; ignore generated content.
data/db_recon/????-??-??/
data/db_recon/manifest.json
data/Athena_mig/

# Python bytecode
*.pyc
__pycache__/

# Puppeteer screenshots (generated artefacts)
temporary screenshots/

# OS
.DS_Store
Thumbs.db
